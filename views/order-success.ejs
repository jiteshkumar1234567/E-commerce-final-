<%- include('./partials/header') %>

<section class="min-h-screen flex items-center justify-center px-6
 bg-gradient-to-br from-indigo-700 via-purple-700 to-pink-600 text-white">

  <div class="relative max-w-lg w-full text-center
              bg-white/10 backdrop-blur-3xl
              border border-white/20 rounded-3xl
              shadow-2xl p-8 md:p-12 overflow-hidden">

    <!-- GLOW EFFECTS -->
    <div class="absolute w-72 h-72 md:w-96 md:h-96 bg-yellow-400/30 blur-[120px] md:blur-[150px] rounded-full -top-16 -left-16 md:-top-20 md:-left-20"></div>
    <div class="absolute w-72 h-72 md:w-96 md:h-96 bg-pink-400/30 blur-[120px] md:blur-[150px] rounded-full -bottom-16 -right-16 md:-bottom-20 md:-right-20"></div>

    <!-- CHECK ICON -->
    <div class="relative z-10 flex justify-center mb-6">
      <div class="w-16 h-16 md:w-20 md:h-20 rounded-full
                  bg-gradient-to-r from-yellow-300 to-orange-400
                  flex items-center justify-center
                  animate-bounce">
        <i class="ri-check-line text-3xl md:text-4xl text-zinc-900"></i>
      </div>
    </div>

    <!-- HEADLINE -->
    <h1 class="relative z-10 text-3xl md:text-4xl font-extrabold mb-4 leading-tight">
      Order Placed
      <span class="bg-gradient-to-r from-yellow-300 to-orange-400
                   bg-clip-text text-transparent">
        Successfully
      </span>
    </h1>

    <!-- MESSAGE -->
    <p class="relative z-10 text-white/80 text-base md:text-lg leading-relaxed">
      Thank you for shopping with <span class="font-semibold text-white">Scatch</span>.
      <br>
      Your premium bag is on its way 👜✨
    </p>

    <!-- BUTTONS -->
    <div class="relative z-10 mt-8 md:mt-10 flex flex-col sm:flex-row gap-4 justify-center">
      <a href="/products"
         class="px-6 md:px-8 py-3 md:py-4 rounded-xl font-semibold
                bg-gradient-to-r from-yellow-300 to-orange-400
                text-zinc-900 hover:scale-105 transition">
        Continue Shopping
      </a>

      <a href="/"
         class="px-6 md:px-8 py-3 md:py-4 rounded-xl
                bg-white/10 hover:bg-white/20
                transition">
        Go to Home
      </a>
    </div>

  </div>

</section>

<%- include('./partials/footer') %>
