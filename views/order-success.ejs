<%- include('./partials/header') %>

<section class="min-h-screen flex items-center justify-center px-6
 bg-gradient-to-br from-indigo-700 via-purple-700 to-pink-600 text-white">

  <div class="relative max-w-lg w-full text-center
              bg-white/10 backdrop-blur-3xl
              border border-white/20 rounded-3xl
              shadow-2xl p-12 overflow-hidden">

    <!-- GLOW -->
    <div class="absolute w-96 h-96 bg-yellow-400/30 blur-[150px] rounded-full -top-20 -left-20"></div>
    <div class="absolute w-96 h-96 bg-pink-400/30 blur-[150px] rounded-full -bottom-20 -right-20"></div>

    <!-- CHECK ICON -->
    <div class="relative z-10 flex justify-center mb-6">
      <div class="w-20 h-20 rounded-full
                  bg-gradient-to-r from-yellow-300 to-orange-400
                  flex items-center justify-center
                  animate-bounce">
        <i class="ri-check-line text-4xl text-zinc-900"></i>
      </div>
    </div>

    <!-- TEXT -->
    <h1 class="relative z-10 text-4xl font-extrabold mb-4">
      Order Placed
      <span class="bg-gradient-to-r from-yellow-300 to-orange-400
                   bg-clip-text text-transparent">
        Successfully
      </span>
    </h1>

    <p class="relative z-10 text-white/80 text-lg leading-relaxed">
      Thank you for shopping with <span class="font-semibold text-white">Scatch</span>.
      <br>
      Your premium bag is on its way 👜✨
    </p>

    <!-- BUTTONS -->
    <div class="relative z-10 mt-10 flex flex-col sm:flex-row gap-4 justify-center">

      <a href="/products"
         class="px-8 py-4 rounded-xl font-semibold
                bg-gradient-to-r from-yellow-300 to-orange-400
                text-zinc-900 hover:scale-105 transition">
        Continue Shopping
      </a>

      <a href="/"
         class="px-8 py-4 rounded-xl
                bg-white/10 hover:bg-white/20
                transition">
        Go to Home
      </a>

    </div>

  </div>

</section>

<%- include('./partials/footer') %>
