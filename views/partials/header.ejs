<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Scatch | Electo</title>

  <!-- Remix Icons -->
  <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet">

  <!-- Tailwind -->
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gradient-to-br from-indigo-700 via-purple-700 to-pink-600 text-white">

<!-- ================= NAVBAR ================= -->
<nav class="fixed top-0 left-0 w-full z-50 backdrop-blur-2xl bg-white/10 border-b border-white/20">
  <div class="max-w-7xl mx-auto px-6 py-4 flex items-center justify-between">

    <!-- LOGO -->
    <a href="/" class="flex items-center gap-3">
      <div class="w-10 h-10 rounded-xl bg-gradient-to-br from-yellow-300 to-orange-400
                  flex items-center justify-center">
        <i class="ri-shopping-bag-3-fill text-zinc-900 text-xl"></i>
      </div>
      <h2 class="text-2xl font-extrabold">Scatch</h2>
    </a>

    <!-- LINKS -->
 <div class="hidden md:flex gap-10 font-medium text-white/90">
  
  <a href="/" class="hover:text-yellow-300 transition">Home</a>
  <a href="/products" class="hover:text-yellow-300 transition">Products</a>

  <!-- My Orders link for logged-in users -->
  <% if (user) { %>
    <a href="/users/my-orders">My Orders</a>
  <% } %>

  <!-- Cart ONLY for users -->
  <% if (user) { %>
    <a href="/cart" class="relative flex items-center gap-2 group">

      <!-- Cart Icon -->
      <i class="ri-shopping-cart-2-line text-xl group-hover:text-yellow-300 transition"></i>

      <!-- Text -->
      <span class="hidden md:inline-block group-hover:text-yellow-300 font-medium">
        Cart
      </span>

      <!-- Count Badge -->
      <% if(cartCount > 0){ %>
        <span id="cart-count" 
              class="absolute -top-2 -right-3
                     w-5 h-5 rounded-full
                     bg-gradient-to-r from-yellow-400 to-orange-500
                     text-black text-xs font-bold
                     flex items-center justify-center">
          <%= cartCount %>
        </span>
      <% } else { %>
        <span id="cart-count" class="hidden absolute -top-2 -right-3
                     w-5 h-5 rounded-full
                     bg-gradient-to-r from-yellow-400 to-orange-500
                     text-black text-xs font-bold
                     flex items-center justify-center">
          0
        </span>
      <% } %>

    </a>
  <% } %>

</div>


<!-- ACTIONS -->
<div class="flex gap-4 items-center">
  <% if (owner) { %>
    <!-- Dashboard link for owner -->
    <a href="/owners/dashboard"
       class="px-5 py-2 rounded-xl bg-yellow-400 text-black font-semibold hover:bg-yellow-500 transition">
      Dashboard
    </a>

    <a href="/owners/dashboard" class="flex items-center gap-2 px-5 py-2 rounded-xl
                             bg-white/10 text-white font-semibold
                             hover:bg-white/20 transition">
      Hello, <%= owner.fullname %>
    </a>

    <a href="/users/logout"
       class="px-5 py-2 rounded-xl bg-red-500 text-white font-semibold hover:bg-red-600 transition">
      Logout
    </a>

  <% } else if (user) { %>
    <a href="/users/profile" class="flex items-center gap-2 px-5 py-2 rounded-xl
                             bg-white/10 text-white font-semibold
                             hover:bg-white/20 transition">
      Hello, <%= user.fullname %>
    </a>

    <a href="/users/logout"
       class="px-5 py-2 rounded-xl bg-red-500 text-white font-semibold hover:bg-red-600 transition">
      Logout
    </a>

  <% } else { %>
    <a href="/users/login"
       class="px-5 py-2 rounded-xl bg-white/10 text-white hover:bg-white/20 transition">
      Login
    </a>

    <a href="/users/register"
       class="px-5 py-2 rounded-xl font-semibold text-zinc-900
              bg-gradient-to-r from-yellow-300 to-orange-400 hover:shadow-xl transition">
      Register
    </a>
  <% } %>
</div>

  </div>
</nav>
