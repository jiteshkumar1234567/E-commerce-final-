<%- include('./partials/header') %>

<!-- ================= HERO SECTION ================= -->
<section class="min-h-screen flex items-center justify-center pt-28 px-6 md:pt-32">
  <div class="max-w-7xl mx-auto grid grid-cols-1 md:grid-cols-2 gap-12 md:gap-14 items-center">

    <!-- TEXT -->
    <div class="text-center md:text-left">
      <span class="inline-block mb-6 px-6 py-2 rounded-full bg-white/20 text-sm backdrop-blur">
        ⚡ Smart Electronics Store
      </span>

      <h1 class="text-4xl sm:text-5xl md:text-6xl font-extrabold leading-tight">
        Power Your Life.<br>
        <span class="bg-gradient-to-r from-cyan-300 to-blue-400 bg-clip-text text-transparent">
          With Smart Tech.
        </span>
      </h1>

      <p class="mt-6 text-base sm:text-lg text-white/80 max-w-xl mx-auto md:mx-0">
        Discover latest smartphones, laptops, accessories & smart gadgets
        built for speed, performance and everyday innovation.
      </p>

      <!-- BUTTONS -->
      <div class="mt-8 md:mt-10 flex flex-col sm:flex-row gap-4 sm:gap-6 justify-center md:justify-start">
        <a href="/products"
           class="px-8 py-4 rounded-xl font-semibold text-zinc-900
                  bg-gradient-to-r from-cyan-300 to-blue-400
                  hover:scale-105 hover:shadow-2xl hover:shadow-blue-400/40
                  transition-all duration-300 text-center">
          Shop Electronics
        </a>

        <% if (!user && !owner) { %>
          <a href="/users/register"
             class="px-8 py-4 rounded-xl bg-white/20 backdrop-blur
                    hover:bg-white/30 hover:scale-105 transition-all duration-300 text-center">
            Create Account
          </a>
        <% } else { %>
          <a href="/Products"
             class="px-8 py-4 rounded-xl bg-white/20 backdrop-blur
                    hover:bg-white/30 hover:scale-105 transition-all duration-300 text-center">
            Go to Products
          </a>
        <% } %>
      </div>
    </div>

    <!-- IMAGES -->
    <div class="relative grid grid-cols-2 gap-4 sm:gap-6 mt-10 md:mt-0">

      <div class="absolute inset-0 bg-gradient-to-tr from-cyan-400/20 to-blue-500/20 blur-3xl -z-10"></div>

      <div class="image-card animate-float">
        <img src="https://plus.unsplash.com/premium_photo-1726880618173-174e2b9f4e13?q=80&w=1169&auto=format&fit=crop" alt="Smartphone">
        <span class="tag">Smartphone</span>
      </div>

      <div class="image-card animate-float delay-200">
        <img src="https://images.unsplash.com/photo-1646500366920-b4c5ce29237d?q=80&w=1170&auto=format&fit=crop" alt="Laptop">
        <span class="tag">Laptop</span>
      </div>

      <div class="image-card animate-float delay-400">
        <img src="https://images.unsplash.com/photo-1580894908361-967195033215" alt="Headphones">
        <span class="tag">Headphones</span>
      </div>

      <div class="image-card animate-float delay-600">
        <img src="https://images.unsplash.com/photo-1709859071282-9982d4d7fe81?q=80&w=1170&auto=format&fit=crop" alt="Smart Watch">
        <span class="tag">Smart Watch</span>
      </div>

    </div>
  </div>
</section>

<!-- ================= FEATURES SECTION ================= -->
<section class="py-16 sm:py-20 lg:py-24 px-6 bg-white/5 backdrop-blur-xl">
  <div class="max-w-7xl mx-auto text-center">

    <h2 class="text-3xl sm:text-4xl font-extrabold mb-12 sm:mb-16">
      Why Buy From <span class="text-cyan-300">Scatch Electronics</span>?
    </h2>

    <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-4 gap-8 sm:gap-10">

      <div class="p-6 sm:p-8 rounded-2xl bg-white/10 backdrop-blur
                  hover:scale-105 hover:bg-white/20 transition-all duration-300">
        <i class="ri-shield-check-line text-4xl text-cyan-300"></i>
        <h4 class="mt-4 text-lg sm:text-xl font-semibold">1 Year Warranty</h4>
        <p class="mt-2 text-white/70 text-sm sm:text-base">
          Genuine products with official brand warranty.
        </p>
      </div>

      <div class="p-6 sm:p-8 rounded-2xl bg-white/10 backdrop-blur
                  hover:scale-105 hover:bg-white/20 transition-all duration-300">
        <i class="ri-flashlight-line text-4xl text-cyan-300"></i>
        <h4 class="mt-4 text-lg sm:text-xl font-semibold">Fast Delivery</h4>
        <p class="mt-2 text-white/70 text-sm sm:text-base">
          Quick & secure delivery across India.
        </p>
      </div>

      <div class="p-6 sm:p-8 rounded-2xl bg-white/10 backdrop-blur
                  hover:scale-105 hover:bg-white/20 transition-all duration-300">
        <i class="ri-secure-payment-line text-4xl text-cyan-300"></i>
        <h4 class="mt-4 text-lg sm:text-xl font-semibold">Secure Payments</h4>
        <p class="mt-2 text-white/70 text-sm sm:text-base">
          UPI, Cards, NetBanking & COD supported.
        </p>
      </div>

      <div class="p-6 sm:p-8 rounded-2xl bg-white/10 backdrop-blur
                  hover:scale-105 hover:bg-white/20 transition-all duration-300">
        <i class="ri-cpu-line text-4xl text-cyan-300"></i>
        <h4 class="mt-4 text-lg sm:text-xl font-semibold">Latest Technology</h4>
        <p class="mt-2 text-white/70 text-sm sm:text-base">
          New launches & trending gadgets available.
        </p>
      </div>

    </div>
  </div>
</section>

<style>
@keyframes float {
  0%,100% { transform: translateY(0); }
  50% { transform: translateY(-15px); }
}

.animate-float {
  animation: float 6s ease-in-out infinite;
}
.delay-200 { animation-delay: .2s; }
.delay-400 { animation-delay: .4s; }
.delay-600 { animation-delay: .6s; }

.image-card {
  position: relative;
  border-radius: 1.5rem;
  overflow: hidden;
  background: rgba(255,255,255,0.08);
  backdrop-filter: blur(14px);
  box-shadow: 0 20px 40px rgba(0,0,0,0.35);
  transition: all .4s ease;
  height: 200px; /* default for mobile */
}

@media (min-width: 640px) {
  .image-card { height: 220px; }
}
@media (min-width: 768px) {
  .image-card { height: 240px; }
}
@media (min-width: 1024px) {
  .image-card { height: 260px; }
}

.image-card img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  filter: blur(1.8px) brightness(.9);
  transform: scale(1.05);
  transition: all .4s ease;
}

.image-card:hover img {
  filter: blur(0) brightness(1);
  transform: scale(1.1);
}

.image-card::after {
  content: "";
  position: absolute;
  inset: 0;
  background: linear-gradient(to top, rgba(0,0,0,.45), transparent);
}

.image-card:hover {
  transform: translateY(-12px);
  box-shadow: 0 0 40px rgba(56,189,248,.5);
}

.tag {
  position: absolute;
  bottom: 10px;
  left: 10px;
  padding: 4px 12px;
  font-size: .7rem;
  border-radius: 999px;
  background: rgba(255,255,255,0.25);
  backdrop-filter: blur(8px);
  z-index: 2;
}
</style>

<%- include('./partials/footer') %>
